import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'prayer2calendar',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
